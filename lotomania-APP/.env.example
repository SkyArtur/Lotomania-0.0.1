# Relative path: works in Docker (nginx proxies /api-lotomania to the api
# container) and in `npm run dev` (Vite dev server proxies the same path to
# the local Django server - see vite.config.js). No IP/host to maintain.
VITE_API_URL=/api-lotomania
